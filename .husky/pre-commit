#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running ESLint..."
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ ESLint check failed. Fix errors before committing."
  exit 1
fi

echo "✅ ESLint passed"
